name: Verify Human Identity

on:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Start
        run: echo "Start verification"

      - name: Set Identity
        run: echo '{"name":"shznayurika77","description":"AI Agent shznayurika77"}'

      - name: Done
        run: echo "Verification complete"
